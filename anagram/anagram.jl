function detect_anagrams(subject, candidates)
    for candidate ∈ candidates
        for candidate_letter ∈ candidate
            if candidate_letter ∉ subject
                continue
            else
                N += n
            end
        end
    end
end
